import React from 'react';

type HeadingProps = {
    children: string
}

const Heading = (props: HeadingProps) => {
  return (
    <div>
      PlaceHolder Text!
    </div>
  );
}

export default Heading;

// Passing texts as children props...